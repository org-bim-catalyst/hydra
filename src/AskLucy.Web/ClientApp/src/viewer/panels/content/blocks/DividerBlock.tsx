import { Divider } from '@mui/material'

/** contracts/content-vocabulary.md "divider" block — a structural separator. No fields. */
export function DividerBlockRenderer() {
  return <Divider sx={{ my: 0.5 }} />
}
